import 'package:flutter/material.dart';

class GoalsScreen extends StatelessWidget {
  const GoalsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    // Note: The main.dart file already contains a placeholder for GoalsScreen.
    // However, it's good practice to have the real file if you import it.
    return const Center(child: Text('Goals Screen - Work on your objectives here!'));
  }
}
