import 'package:flutter/material.dart';

// Aqua Pop Palette (Matching the provided image)
class AppColors {
  // Primary: Header/AppBar, Main Buttons, Progress Bar (Light Aqua Blue: #74C6E7)
  static const Color primaryColor = Color(0xFF74C6E7); 

  // Secondary: Card Backgrounds, UI Surfaces (Very Pale Aqua/Mint: #EAF4F4)
  static const Color secondary = Color(0xFFEAF4F4); 

  // Accent: "Need a Quick Boost" Button, Sun Icon (Vibrant Orange/Yellow: #FCC34C)
  static const Color accent = Color(0xFFFCC34C); 

  // Background: Main Screen Background (Off-White/Lightest Gray: #F8F8F8)
  static const Color background = Color(0xFFF8F8F8); 

  // Text Colors
  static const Color textDark = Color(0xFF333333); 
  static const Color textSubtle = Color(0xFF707070); 

  // Utility Colors (Used for chips, icons, error handling)
  static const Color success = Color(0xFF4CAF50);
  static const Color error = Color(0xFFF44336);
  static const Color warning = Color(0xFFFF9800);
}
